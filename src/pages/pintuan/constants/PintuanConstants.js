import keyMirror from 'keymirror';
import assign from 'object-assign';
import { BaseConstants } from '../../../base/';

/**
 * 常量
 * eg: TOGGLE_SHAKE: null,
 */
export default assign({}, BaseConstants, keyMirror({
    //TODO do yourself
}));
